entier tableau[3], entier a, entier b, entier c;

main() {
  tableau[0] = 2;
  tableau[1] = 5;
  tableau[2] = 12;
  a = tableau[0];
  b = tableau[1];
  c = tableau[2];
  ecrire(tableau[0]);
  ecrire(tableau[1]);
  ecrire(tableau[2]);
  ecrire(a);
  ecrire(b);
  ecrire(c);
}
